<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View -->
    <record id="view_property_import_wizard_form" model="ir.ui.view">
        <field name="name">property.import.wizard.form</field>
        <field name="model">property.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Import Property">
                <sheet>
                    <group>
                        <field name="natural_language_request" placeholder="Describe the property or search criteria in natural language. For example: 'Find a 3-bedroom house in Seattle under $500,000' or 'Create a property at 123 Main St, New York with 2 bedrooms and 2 bathrooms priced at $350,000'"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_process_request" string="Process" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_property_import_wizard" model="ir.actions.act_window">
        <field name="name">Import Property</field>
        <field name="res_model">property.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_property_import_wizard_form"/>
    </record>

</odoo>